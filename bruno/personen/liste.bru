meta {
  name: Personen – Liste
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/personen
  body: none
  auth: none
}

query {
  nachname:
  geburtsname:
  geburtsjahr:
}

tests {
  test("Status 200", function() {
    expect(res.status).to.equal(200);
    expect(res.body.data).to.be.an("array");
  });
}
