meta {
  name: Personen – Lesen
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/personen/{{personId}}
  body: none
  auth: none
}

tests {
  test("Status 200", function() {
    expect(res.status).to.equal(200);
    expect(res.body.data.id).to.equal(bru.getVar("personId"));
  });
}
