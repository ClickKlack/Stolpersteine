meta {
  name: Suche – Filter
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/suche
  body: none
  auth: none
}

query {
  stadtteil: Altstadt
  ohne_wikidata: 1
}

tests {
  test("Status 200", function() {
    expect(res.status).to.equal(200);
    expect(res.body.data.treffer).to.be.a("number");
  });
}
