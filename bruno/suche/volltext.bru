meta {
  name: Suche – Volltext
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/suche
  body: none
  auth: none
}

query {
  q: Schmidt
}

tests {
  test("Status 200", function() {
    expect(res.status).to.equal(200);
    expect(res.body.data).to.have.property("treffer");
  });
}
