meta {
  name: Templates – Aktualisieren
  type: http
  seq: 3
}

put {
  url: {{baseUrl}}/templates/1
  body: json
  auth: none
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "inhalt": "Die '''Liste der Stolpersteine in [[SEITE.STADTTEIL_WIKIPEDIA_LINK]]'''..."
  }
}

tests {
  test("Status 200", function() {
    expect(res.status).to.equal(200);
    expect(res.body.data).to.have.property("version");
  });
}
