meta {
  name: Templates – Lesen
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/templates/1
  body: none
  auth: none
}

tests {
  test("Status 200", function() {
    expect(res.status).to.equal(200);
    expect(res.body.data).to.have.property("inhalt");
    expect(res.body.data).to.have.property("version");
  });
}
