meta {
  name: Logout
  type: http
  seq: 3
}

post {
  url: {{baseUrl}}/auth/logout
  body: none
  auth: none
}

tests {
  test("Logout erfolgreich", function() {
    expect(res.status).to.equal(204);
  });
}
