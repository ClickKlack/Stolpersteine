meta {
  name: Stolpersteine – Liste
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/stolpersteine
  body: none
  auth: none
}

query {
  status:
  zustand:
  stadtteil:
  strasse:
  person_id:
  verlegeort_id:
  foto_status:
  ohne_wikidata:
}

tests {
  test("Status 200", function() {
    expect(res.status).to.equal(200);
    expect(res.body.data).to.be.an("array");
  });
}
