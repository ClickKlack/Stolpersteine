meta {
  name: Adressen – Lokationen Liste
  type: http
  seq: 9
}

get {
  url: {{baseUrl}}/adressen/alle-lokationen
  body: none
  auth: none
}

query {
  strasse_id:
  stadtteil_id:
  plz_id:
}

tests {
  test("Status 200", function() {
    expect(res.status).to.equal(200);
    expect(res.body.data).to.be.an("array");
  });
}
