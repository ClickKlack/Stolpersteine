meta {
  name: Adressen – Städte Liste
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/adressen/staedte
  body: none
  auth: none
}

tests {
  test("Status 200", function() {
    expect(res.status).to.equal(200);
    expect(res.body.data).to.be.an("array");
  });
}
