meta {
  name: Adressen – PLZ Liste
  type: http
  seq: 7
}

get {
  url: {{baseUrl}}/adressen/alle-plz
  body: none
  auth: none
}

query {
  stadt_id:
}

tests {
  test("Status 200", function() {
    expect(res.status).to.equal(200);
    expect(res.body.data).to.be.an("array");
  });
}
