meta {
  name: Adressen – Straßen Liste
  type: http
  seq: 5
}

get {
  url: {{baseUrl}}/adressen/alle-strassen
  body: none
  auth: none
}

query {
  stadt_id:
  q:
}

tests {
  test("Status 200", function() {
    expect(res.status).to.equal(200);
    expect(res.body.data).to.be.an("array");
  });
}
